<%= turbo_stream.replace 'project_users_count', @project.project_users.count %>
<%= turbo_stream.prepend "project_users", @project_user %>
<%= turbo_stream.update ProjectUser.new(project: @project), "" %>
