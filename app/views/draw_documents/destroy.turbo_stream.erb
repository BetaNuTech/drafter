<%= turbo_stream.remove @draw_document %>
<%= turbo_stream.update dom_id(@draw, :submitted_documents), @draw.required_documents.count %>
<%= turbo_stream.update dom_id(@draw, :action_buttons), partial: 'draws/action_buttons', locals: { draw: @draw } %>
