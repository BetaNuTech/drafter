<%= turbo_stream.update dom_id(@draw_cost.draw, :total_cost), number_to_currency(@draw_cost.draw.draw_cost_total) %>
<%= turbo_stream.remove @draw_cost %>
<%= turbo_stream.update dom_id(@draw_cost.draw, :action_buttons), partial: 'draws/action_buttons', locals: { draw: @draw_cost.draw } %>
