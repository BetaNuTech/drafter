<%= turbo_frame_tag 'new_invoice', draw_cost do %>
  <%= link_to('Add Invoice', new_draw_cost_invoice_path(draw_cost_id: draw_cost.id), class: 'btn btn-primary', data: {turbo_frame: dom_id(draw_cost,'new_invoice')}) if policy(Invoice.new(draw_cost: draw_cost)).new? %>
<% end %>
