<div class="row">
  <div class="col-auto">
    <div class="jumbotron">
      <h2 class="display-4">
        <%= project.name %>
        <small>
          <%= link_to(image_tag(edit_image), edit_project_path(project), class: 'action-link')%>
        </small>
      </h2>
      <p class="lead">
        <b>Budget:</b> <%= number_to_currency(project.budget) %>
      <br/>
      <%= project.description %>
    </div>
  </div>
  <div class="col-auto">
  </div>
</div>
