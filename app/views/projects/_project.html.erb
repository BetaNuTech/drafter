<% if current_user.project_developer?(project) %>
  <%= render partial: 'project_developer', locals: {project: project} %>
<% else %>
  <%= render partial: 'project_default', locals: {project: project} %>
<% end %>
